# Parity fixture

A paragraph with **bold**, *italic*, ~~strike~~, `inline code`, a
[link](https://example.com) and inline math $\alpha^2 + \beta$.

## Lists

- first item that is long enough to wrap around the width boundary
- second item
  - nested item with `code`
  - another nested
- [x] done task
- [ ] open task

1. ordered one
2. ordered two
   1. nested ordered
   2. nested ordered 2

A loose list:

- one

- two

> A quote with **bold** text and enough words to wrap at least once.
>
> > nested quote

### Code

```rust
fn main() {
    println!("hello");
}
```

```python
def greet(name: str) -> None:
    print(f"hi {name}")
```

```
plain fence with a very long line that has to wrap at narrow widths because it keeps going
```

| Name | Value | Notes |
|------|-------|-------|
| a | 1 | short |
| b | 2 | a longer note that wraps |
| c | 3 | ok |

---

Final paragraph.
